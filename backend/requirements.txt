# requirements.txt - Backend dependencies

# FastAPI và server
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Word processing (từ requirements.txt gốc)
python-docx==1.1.0
lxml>=4.9.0
Pillow>=10.4.0
olefile==0.47

# Optional: nếu muốn compile LaTeX trên server
# python-pdflatex==0.1.3
